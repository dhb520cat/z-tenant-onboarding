# credentials — never commit
.secrets/
*.env
!*.env.example

# build output
target/
node_modules/

# upstream clone, kept locally for reference only (not ours to redistribute)
reference-impl/

# offline docs snapshot used during the build
docs/

# internal drafts, not for publication
x-post-draft.md
SUBMISSION.md.bak
